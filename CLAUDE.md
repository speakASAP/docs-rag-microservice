# Claude Instructions - docs-rag-microservice

Canonical repository instructions are in [`AGENTS.md`](AGENTS.md).

Read `../shared/docs/DOCUMENTATION_AUTHORITY.md`, `SYSTEM.md`,
`docs/SOURCE_OF_TRUTH.md`, `TASKS.md` and `STATE.json` before changing
ingestion or retrieval.

docs-RAG is a derived semantic projection of Git. It must never become an
independent documentation store, index copied ecosystem snapshots, or replace
IPS graph-first traceability.
