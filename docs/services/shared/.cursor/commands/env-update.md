# env-update

The user **authorizes applying the last proposed `.env` change** (exact lines the agent already showed).

Follow the project skill **env-update**: `.cursor/skills/env-update/SKILL.md` — backup secrets file, `touch /tmp/.claude-env-edit-approved`, then edit only that proposal; sync `.env.example` keys if required; never print secret values.
